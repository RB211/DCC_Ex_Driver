# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# Editor / Claude Code local settings (machine-specific paths, not shareable)
.vscode/
.claude/settings.local.json

# build.sh output — regenerate with the toolchain, don't track
Documents/out/
Documents/parts/
